import { IonHeader, IonTitle, IonToolbar } from "@ionic/react";

const ContentTitle = () => {
  return (
    <IonHeader collapse="condense">
      <IonToolbar>
        <IonTitle size="large">NUR MAX</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default ContentTitle;
